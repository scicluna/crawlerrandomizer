class Crawler:
    def __init__(self, crawler_class, level):
        self.crawler_class = crawler_class
        self.level = level
        # Add more attributes as needed (e.g., equipment, skills, stats)